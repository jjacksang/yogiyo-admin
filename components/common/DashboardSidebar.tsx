import React from 'react'

type Props = {}

const DashboardSidebar = (props: Props) => {
    return (
      <div style={{ width: '260px', height: '100%', background: '#f0f2f5' }}> {/* 여기에 배경색 등 스타일 추가 가능 */}
        
      </div>
    );
  };

export default DashboardSidebar