export interface IMainMenu {
    signatureMenus: [
        {
            id: number;
            name: string;
            content: string;
            picture: string;
            price: number;
        }
    ];
}
